---
description: Start a new change request — capture it, clarify, push back where needed, and triage it into a tier (TRIVIAL/STANDARD/DEEP).
argument-hint: "<what you want to change>"
---

The user is starting a new change request:

$ARGUMENTS

Follow the `sd-intake` skill: register the request with the engine, write
`requests/<id>/intake.md` (your understanding, push-backs, and open questions),
recommend and apply a tier, then tell the user the next step is `/sd:spec`.
