---
description: Show the REQ→STEP→TEST traceability matrix for a request and any coverage holes (read-only).
argument-hint: "[req-id]"
---

Show the traceability matrix for request `$ARGUMENTS` (or the active request). Follow
the `sd-trace` skill: run `engine trace` and summarize each requirement's coverage and
any holes.
